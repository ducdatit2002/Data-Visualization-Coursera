#JS notes
Change things based on user interaction

JS script can be added within <script></script> tags after the body in HTML page.

# variables and constants
var name ='John'; (older versions)
let name = 'John'; (newer versions)

Const version = '1.3'; (never changes)

# Comparing operators
let isEqual = months === 4;
= asignemnet
== comapred elements
=== type casting


# functions

function add(v1,v2) {
	return v1+v2
}

let result = add(5,3) # function call


# conditional statements
1. 
if (c1 === 19) {
	color = 'blue'
} else {
	color = 'red'
}

shorter version of the above code:
let color = v1 === 10 ? 'blue' : 'red';

2. 
for(i=1; i <21; i +=1){
	console.log(i)
}


